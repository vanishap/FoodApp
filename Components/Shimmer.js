const Shimmer =()=>{
    return (
        <h1>Loading....</h1>
    )
}

export default Shimmer;

{/* <div className='shimmer-card' key={index}>
        Array(15).fill('').map(card=>)
        </div> */}